apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: odoo-16-exrernal-pvc
  namespace: odoo-16
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: openebs-hostpath
